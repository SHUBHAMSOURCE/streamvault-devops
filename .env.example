# ---- Frontend ----
REACT_APP_API_URL=https://streamvault.local/api
REACT_APP_ENV=development

# ---- Backend ----
PORT=5000
NODE_ENV=development

# Database (placeholder — replace with real values, never commit secrets)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=streamvault
DB_USER=changeme
DB_PASSWORD=changeme

# JWT / auth
JWT_SECRET=changeme

# ---- Cloud (used in Terraform/IAM phase) ----
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=changeme
AWS_SECRET_ACCESS_KEY=changeme

# ---- Monitoring ----
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=changeme
