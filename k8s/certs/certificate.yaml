apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: streamvault-tls
  namespace: default
spec:
  secretName: streamvault-tls-secret
  issuerRef:
    name: selfsigned-issuer
    kind: Issuer
  dnsNames:
    - streamvault.local
